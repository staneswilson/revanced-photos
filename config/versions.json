{
  "gphotos": {
    "version": "",
    "updatedAt": "2026-05-09T00:00:00Z",
    "source": "apkpure",
    "note": "Empty version = pull latest from APKPure each build. Unlimited storage is gated by device fingerprint (handled by spoof-features), not app version. Set GPHOTOS_VERSION env var to override per build."
  }
}
